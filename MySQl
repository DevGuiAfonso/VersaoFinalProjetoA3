# passo a passo para rodar o código em MySQL
# 1. Instale o MySQL Server em sua máquina.
# 2. Abra o MySQL Workbench ou qualquer outro cliente MySQL que você preferir.
# 3. Crie um novo banco de dados com o nome "sistemadeeventos".
# 4. Execute o seguinte comando SQL para criar a tabela "eventos"

# CREATE TABLE evento (
#    id INT AUTO_INCREMENT PRIMARY KEY,
#    nome VARCHAR(100) NOT NULL,
#    local VARCHAR(100) NOT NULL,
#    data DATE NOT NULL,
#    horario TIME NOT NULL,
#    categoria VARCHAR(50) NOT NULL,
#    descricao TEXT,
#    dataHora DATETIME NOT NULL
#);

# 5. Insira alguns dados de exemplo na tabela "eventos" 